apiVersion: v1
kind: Service
metadata:
  name: {{ include "navidrome-scim.fullname" . }}
  labels:
    {{- include "navidrome-scim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "navidrome-scim.selectorLabels" . | nindent 4 }}
